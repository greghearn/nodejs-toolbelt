'use strict'

const CALL_CHAIN_LENGTH = 'call-chain-length'
const CORRELATION_ID = 'uuid'
const DEBUG_ENABLED = 'debug-enabled'

module.exports = {
  CALL_CHAIN_LENGTH,
  CORRELATION_ID,
  DEBUG_ENABLED
}
